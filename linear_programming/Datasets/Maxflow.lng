 MODEL:
SETS:
NODES/1..5/;
ARCS(NODES,NODES)/1,2 1,3 2,3 2,4 3,5 4,5  5,1/
:CAP,FLOW;
ENDSETS
MAX=FLOW(5,1);
@FOR(ARCS(I,J):FLOW(I,J)<CAP(I,J));
@FOR(NODES(I):@SUM(ARCS(J,I):FLOW(J,I))
=@SUM(ARCS(I,J):FLOW(I,J)));
DATA:
CAP=2,3,3,4,2,1,1000;
ENDDATA
 END
